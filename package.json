{
  "name": "online-inclinometer",
  "version": "1.0.0",
  "description": "Online Inclinometer - Measure surface angles with your mobile device",
  "private": true,
  "scripts": {
    "dev": "http-server -S -C .certs/localhost.pem -K .certs/localhost-key.pem -p 8443 -a 0.0.0.0 -c-1",
    "dev:local": "http-server -S -C .certs/localhost.pem -K .certs/localhost-key.pem -p 8443 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
